#include <stdio.h>

int main(void)
{
    int n, S[n];
    for (int i = 0; i < n; i++)
    {
        fscanf(stdin, "%d", &S[i]);
    }
    char operation;
    int i, j;
    while (scanf("%c %d %d",
                 &operation, &i, &j) == 3)
    {
        if (operation == "s")
        {
        }
        else
        {
        }
    }
    return 0;
}